<div class="streaming-indicator" data-streaming-indicator hidden role="status" aria-live="polite">
  <span class="streaming-dots" aria-hidden="true"><i></i><i></i><i></i></span>
  <span>Assistant is typing…</span>
  <button type="button" data-streaming-cancel>Stop</button>
</div>

<style>
  .streaming-indicator { display: flex; align-items: center; gap: .45rem; padding: .35rem 1rem .5rem; color: #a5b4fc; font-size: .72rem; }
  .streaming-indicator button { margin-left: auto; padding: .2rem .45rem; border: 1px solid currentColor; border-radius: .35rem; color: inherit; background: transparent; cursor: pointer; font: inherit; font-size: .68rem; }
  .streaming-dots { display: inline-flex; gap: .18rem; }
  .streaming-dots i { width: .3rem; height: .3rem; border-radius: 50%; background: currentColor; animation: streaming-dot 1s infinite ease-in-out; }
  .streaming-dots i:nth-child(2) { animation-delay: .15s; }.streaming-dots i:nth-child(3) { animation-delay: .3s; }
  @keyframes streaming-dot { 0%, 60%, 100% { opacity: .35; transform: translateY(0); } 30% { opacity: 1; transform: translateY(-.2rem); } }
  @media (prefers-reduced-motion: reduce) { .streaming-dots i { animation: none; opacity: .7; } }
  @media (max-width: 360px) { .streaming-indicator { padding-inline: .75rem; } }
</style>
